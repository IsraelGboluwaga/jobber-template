# Secrets — never commit
.env

# Your real CV — generated by scripts/import_cv.py, never the template's business.
# data/master_cv.example.json (the schema reference) stays tracked.
data/master_cv.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Local scratch / generated tailored artifacts (Notion is the review surface)
*.log
.DS_Store
